import { Typography } from '@mui/material';
import * as React from 'react';

const PrivatePage: React.FC = () => (
  <Typography variant="h2" component="h2">This is Private Page!</Typography>
);

export default PrivatePage;
