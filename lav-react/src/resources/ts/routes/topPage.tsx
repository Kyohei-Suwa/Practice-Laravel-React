import { Typography } from '@mui/material';
import * as React from 'react';

const FamilyPage: React.FC = () => (
  <Typography variant="h2" component="h2">This is Top Page!</Typography>
);

export default FamilyPage;
